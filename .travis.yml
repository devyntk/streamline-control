language: rust
jobs:
  - os: windows
    before_install: choco install gtk-runtime
  - os: linux
    dist: bionic
    before_install: sudo apt update && sudo apt install gtk+3.0 libpango1.0-dev libatk1.0-dev libgtk-3-dev -y
  - os: osx
    before_install: brew install gtk+3
script: cargo build --release
